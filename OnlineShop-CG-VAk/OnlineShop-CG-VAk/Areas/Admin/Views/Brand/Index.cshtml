@model IEnumerable<Brand>


<div class =" container">

    <div class ="row mt-4 mb-2">
        <div class ="col-md-6">

           <h2>Brnads Dashboard</h2>   
        </div>

        <div class ="col-md-6 text-end">
            <a asp-controller="Brand" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i>  Create Brand</a>
        </div>

    </div>

    <partial name = "_Notification" />

    <table class="table table-hover rounded rounded-3">   
        <thead>
            <tr>
                <th>Sl.No</th>
                <th>Name</th>
                <th>Action</th>          
            </tr>
        </thead>
        <tbody>
            @foreach(var brand in Model.Select((value,index)=>new {index, value}))
            {
             <tr>
                <td>@(brand.index+1)</td>
                <td>@brand.value.Name</td>               
                <td>
                    <a asp-controller="Brand" asp-action="Edit" asp-route-id="@brand.value.Id" class="btn btn-sm btn-secondary"><i class="bi bi-pencil-square"></i></a>
                    <a asp-controller="Brand" asp-action="Delete" asp-route-id="@brand.value.Id" class="btn btn-sm btn-danger"><i class="bi bi-trash3"></i></a>
                </td>
            </tr>
            }

        </tbody>
    </table>
</div>
